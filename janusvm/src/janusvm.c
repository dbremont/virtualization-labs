#include "janusvm.h"

int main(int argc, char *argv[]) {
    // // Initialize the JanusVM
    // janusvm_init();

    // // Load the JanusVM bytecode
    // if (argc < 2) {
    //     fprintf(stderr, "Usage: %s <bytecode_file>\n", argv[0]);
    //     return 1;
    // }
    // janusvm_load_bytecode(argv[1]);

    // // Execute the JanusVM bytecode
    // janusvm_execute();

    // // Clean up and exit
    // janusvm_cleanup();
    return 0;
}